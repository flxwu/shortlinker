{
  "name": "shortlinker",
  "version": "1.0.0",
  "description": "A CLI utility to turn your website into a link shortener for your URLs",
  "main": "index.js",
  "author": "Felix Wu",
  "license": "MIT",
  "scripts": {
    "patch-release": "npm version patch && npm publish && git push --follow-tags"
  }
}
